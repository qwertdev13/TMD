name: Update Journals

on:
  push:
    paths:
      - "journals/**"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: false  # Important! Don't use default HTTPS creds

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Run journal generator
        run: node scripts/process-journals.js

      - name: Commit changes
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add index.html public/journals
          git commit -m "Auto-update journals" || echo "No changes"

      - name: Push changes
        uses: ad-m/github-push-action@v0.6.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
